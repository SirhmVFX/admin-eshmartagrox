export async function register() {
    // Minimal instrumentation hook to prevent Firebase Admin initialization errors
    // This hook is required by Next.js but doesn't need to do anything for this admin panel
}
